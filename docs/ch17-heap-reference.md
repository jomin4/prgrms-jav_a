# 17강 · 자바를 포함한 현대언어는 복합체(객체)를 힙에 생성하고 레퍼런스(리모콘)으로 다루는 방식으로 작동한다, 변수에는 단일값만 넣을 수 있어서

> P3. 자료형 & 메모리 | 출처: slog.gg/p/14146 17강

## 🎯 핵심 개념
# 17강
- 26 04 20, p 14146, 17강, 자바를 포함한 현대언어는 복합체(객체)를 힙에 생성하고 레퍼런스(리모콘)으로 다루는 방식으로 작동한다, 변수에는 단일값만 넣을 수 있어서
- 변수에는 값을 하나만 넣을 수 있다.
- 변수에는 2가지 종류(기본형/참조형)가 있다.
- 기본형 변수에는 값이 직접 저장된다.
  - `int a = 10;`
  - `boolean b = true;`
- 참조형 변수에는 값에 대한 레퍼런스(참조/리모콘)가 저장된다.
  - `String name = "Paul";`
    - `['P' 'a' 'u' 'l']`로 구성된 문자열 문자열객체가 힙에 생성된다.
    - 해당 문자열객체를 조종할 수 있는 리모콘(레퍼런스)가 생성되고 그것이 `name` 변수에 저장된다.
    - `name` 같이 값을 직접저장하지 않는 변수를 레퍼런스 변수라고 한다.

## 💻 실습 코드
이론/환경 강의 (문서로만 기록)

## 🧠 메모리에서 무슨 일이?
(해당 없음 / 정리 예정)

## ✅ 실행 결과
```
(정리 예정)
```

## 📝 회고 / 헷갈렸던 점
-
